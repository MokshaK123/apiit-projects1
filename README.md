# apiit-projects1
## _Hotel booking core system

clone the repository and run the following commands

```sh
cd apiit-projects1
npm install
node run prod
php artisan db:wipe
php artisan migrate
php artisan db:seed
php artisan serve
```
